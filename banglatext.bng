~ এটি একটি single-line comment

পূর্ণ বয়স = ২০;

সত্যমিথ্যা ছাত্র = সত্য;

পূর্ণ সংখ্যা = বয়স + ১০;

লিখো(সংখ্যা);

শর্ত (বয়স > ১৮)
{
    লিখো(বয়স);
}
অন্যথা
{
    লিখো(০);
}

চলমান (বয়স < ৩০)
{
    বয়স = বয়স + ১;
}